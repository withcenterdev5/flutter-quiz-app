/// Abstract repository interface for the quiz feature.
///
/// Defines the contract that the data layer must implement.
/// The presentation layer depends only on this abstraction —
/// never on [QuizRepositoryImpl] directly.
///
/// Full method signatures are added in Phase 1 (Task 1.2).
abstract class QuizRepository {}